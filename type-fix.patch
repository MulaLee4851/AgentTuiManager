diff --git a/electron/session-controller.ts b/electron/session-controller.ts
--- a/electron/session-controller.ts
+++ b/electron/session-controller.ts
@@ -345,7 +345,7 @@ export class SessionController {
         const decision = this.approvalPolicy?.decide(approvalCommand)
         const action = decision?.action === 'auto-approve' ? 'allow' : 'ask'
         managed.handle.respondToPermission(event.requestId, action)
-        if (action === 'auto-approve') {
+        if (action === 'allow') {
           managed.summary = reduceSession(managed.summary, { type: 'started' }) as SessionSummary
         } else {
diff --git a/tests/unit/session-controller.test.ts b/tests/unit/session-controller.test.ts
--- a/tests/unit/session-controller.test.ts
+++ b/tests/unit/session-controller.test.ts
@@ -8,6 +8,7 @@ import { ApprovalPolicyEngine } from '../../electron/approval-policy'
 
 class FakeHandle implements HostHandle {
   readonly writes: string[] = []
+  readonly permissionResponses: Array<{ requestId: string; action: 'allow' | 'ask' }> = []
   stops = 0
@@ -32,6 +33,9 @@ class FakeHandle implements HostHandle {
   }
   write(data: string): void { this.writes.push(data) }
   resize(): void {}
+  respondToPermission(requestId: string, action: 'allow' | 'ask'): void {
+    this.permissionResponses.push({ requestId, action })
+  }
   async stop(): Promise<void> { this.stops += 1 }
