diff --git a/src/shared/manager-api.ts b/src/shared/manager-api.ts
--- a/src/shared/manager-api.ts
+++ b/src/shared/manager-api.ts
@@ -78,6 +78,7 @@ export const IPC_CHANNELS = {
   removeApprovalRule: 'agent-manager:remove-approval-rule',
   chooseWorkspace: 'agent-manager:choose-workspace',
   discoverSessions: 'agent-manager:discover-sessions',
+  readClipboardText: 'agent-manager:read-clipboard-text',
   event: 'agent-manager:event',
 } as const
@@ -98,6 +99,7 @@ export interface AgentManagerApi {
   removeApprovalRule(command: string): Promise<void>
   chooseWorkspace(): Promise<string | undefined>
   discoverSessions(agentKind: AgentKind, workspace: string): Promise<NativeSessionSummary[]>
+  readClipboardText(): Promise<string>
   subscribe(listener: (event: ManagerEvent) => void): () => void
 }
diff --git a/electron/preload.ts b/electron/preload.ts
--- a/electron/preload.ts
+++ b/electron/preload.ts
@@ -19,6 +19,7 @@ const api: AgentManagerApi = {
   removeApprovalRule: (command) => ipcRenderer.invoke(IPC_CHANNELS.removeApprovalRule, command),
   chooseWorkspace: () => ipcRenderer.invoke(IPC_CHANNELS.chooseWorkspace),
   discoverSessions: (agentKind, workspace) => ipcRenderer.invoke(IPC_CHANNELS.discoverSessions, agentKind, workspace),
+  readClipboardText: () => ipcRenderer.invoke(IPC_CHANNELS.readClipboardText),
   subscribe: (listener) => {
diff --git a/electron/main.ts b/electron/main.ts
--- a/electron/main.ts
+++ b/electron/main.ts
@@ -1,4 +1,4 @@
-import { app, BrowserWindow, dialog, ipcMain, nativeImage, Tray, type IpcMainInvokeEvent } from 'electron'
+import { app, BrowserWindow, clipboard, dialog, ipcMain, nativeImage, Tray, type IpcMainInvokeEvent } from 'electron'
@@ -244,6 +244,10 @@ function registerIpc(approvalPolicy: ApprovalPolicyStore): void {
     trustedRenderer(event)
     return coalescedDiscovery(validatedAgentKind(kind), workspace(selectedWorkspace))
   })
+  ipcMain.handle(IPC_CHANNELS.readClipboardText, (event) => {
+    trustedRenderer(event)
+    return clipboard.readText('clipboard')
+  })
 }
diff --git a/src/TerminalTile.tsx b/src/TerminalTile.tsx
--- a/src/TerminalTile.tsx
+++ b/src/TerminalTile.tsx
@@ -33,6 +33,16 @@ export default function TerminalTile({ session, detail = false, hidden = false,
       theme: { background: '#090d18', foreground: '#d8e1f5', cursor: '#7c98ff', selectionBackground: '#334269' },
     })
     terminal.open(host)
+    terminal.attachCustomKeyEventHandler((event) => {
+      const isPaste = (event.ctrlKey || event.metaKey) && !event.altKey && event.key.toLocaleLowerCase('en-US') === 'v'
+      if (!isPaste) return true
+      if (event.type === 'keydown') {
+        void window.agentManager.readClipboardText().then((text) => {
+          if (text) terminal.paste(text)
+        }).catch((reason) => setActionError(reason instanceof Error ? reason.message : String(reason)))
+      }
+      return false
+    })
     const input = terminal.onData((data) => { void window.agentManager.write(session.sessionId, data) })
diff --git a/tests/unit/preload.test.ts b/tests/unit/preload.test.ts
--- a/tests/unit/preload.test.ts
+++ b/tests/unit/preload.test.ts
@@ -20,7 +20,7 @@ describe('preload agentManager contract', () => {
   it('exposes only the narrow manager API including native session discovery', async () => {
     await import('../../electron/preload')
     const api = electron.exposeInMainWorld.mock.calls.at(-1)?.[1] as Record<string, (...args: unknown[]) => unknown>
-    expect(Object.keys(api).sort()).toEqual(['acceptApprovalSuggestion', 'addApprovalRule', 'approveSession', 'chooseWorkspace', 'discoverSessions', 'dismissApprovalSuggestion', 'listApprovalRules', 'listAuditEntries', 'listSessions', 'removeApprovalRule', 'removeSession', 'resize', 'restartSession', 'startSession', 'stopSession', 'subscribe', 'write'])
+    expect(Object.keys(api).sort()).toEqual(['acceptApprovalSuggestion', 'addApprovalRule', 'approveSession', 'chooseWorkspace', 'discoverSessions', 'dismissApprovalSuggestion', 'listApprovalRules', 'listAuditEntries', 'listSessions', 'readClipboardText', 'removeApprovalRule', 'removeSession', 'resize', 'restartSession', 'startSession', 'stopSession', 'subscribe', 'write'])
diff --git a/tests/unit/app.test.tsx b/tests/unit/app.test.tsx
--- a/tests/unit/app.test.tsx
+++ b/tests/unit/app.test.tsx
@@ -7,7 +7,8 @@ import type { AgentManagerApi, NativeSessionSummary, SessionSummary } from '../..
 import type { AgentManagerApi, NativeSessionSummary, SessionSummary } from '../../src/shared/manager-api'
 
 const terminalMocks = vi.hoisted(() => ({
-  cols: 80, rows: 24, open: vi.fn(), write: vi.fn(), resize: vi.fn(), dispose: vi.fn(), onData: vi.fn(() => ({ dispose: vi.fn() })),
+  cols: 80, rows: 24, open: vi.fn(), write: vi.fn(), resize: vi.fn(), paste: vi.fn(), dispose: vi.fn(),
+  attachCustomKeyEventHandler: vi.fn(), onData: vi.fn(() => ({ dispose: vi.fn() })),
 }))
@@ -33,6 +34,7 @@ describe('App terminal wall', () => {
       restartSession: vi.fn(), removeSession: vi.fn(),
       acceptApprovalSuggestion: vi.fn(), dismissApprovalSuggestion: vi.fn(),
       listApprovalRules: vi.fn(async () => ['git log --oneline']), addApprovalRule: vi.fn(), removeApprovalRule: vi.fn(),
+      readClipboardText: vi.fn(async () => 'const pasted = true'),
       discoverSessions: vi.fn(async () => [{ id: 'codex-1', title: '修复登录流程', updatedAt: 1_786_000_000_000, workspace: 'B:\\chosen\\workspace' }]),
